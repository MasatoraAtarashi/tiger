#!/usr/bin/expect -f
set timeout 10
spawn node dist/cli.js --no-logo

# Wait for initial render
sleep 2

# Type each character with delay
send "h"
sleep 0.5
send "e"
sleep 0.5
send "l"
sleep 0.5
send "l"
sleep 0.5
send "o"
sleep 1

# Exit
send "\n"
send "/exit\n"
expect eof
